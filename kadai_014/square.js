// 値を二乗する関数
function square(num) {
  const result = num * num; // 二乗の計算
  console.log(result); // 計算結果をコンソールに出力
  return result; // 計算結果を戻り値として返す
}

// 例: 関数を呼び出してみる
square(10); // 100がコンソールに表示される